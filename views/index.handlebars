<h1>Honey Do List</h1>

<div class="image">
    <img src="assets/images/HoneyDoListLogo.png">
</div>

<p>Enter the DO (the task) that you'd like your honey to do.
    <br />When it's done, click on the done button to move the DO into the Done pile.</p>
<hr/>
<div id="uneatenList"
     class="leftSide">
    <h2>DO That Needs Doing</h2>
    <table>
        <!-- bmc: handlebars #each will loop through all the uneatenBurgers in the database -->
        {{#each burger}}
            {{#unless eaten}}
                <tr>
                    <td>

                        {{this.id}}. {{this.food}}

                    </td>
                    <td>
                        <form action="/{{this.id}}?_method=PUT"
                              method="POST">
                            <!-- bmc: button will launch the put method to change the eaten column in the database to true -->
                            <button class='eat-in-database'
                                    id={{this.id}}>This DO is Done!
                            </button>
                        </form>
                    </td>
                </tr>
            {{/unless}}
        {{/each}}
    </table>
    <ul>

    </ul>
</div>
<div id="eatenList"
     class="rightSide">
    <h2>All the DO That's Done</h2>
    <ul>
        <!-- bmc: list of eaten burgers like the list of uneaten ones above -->
        {{#each burger}}
            {{#if eaten}}

            <li>
                {{this.id}}. {{this.food}}
            </li>
            {{/if}}
        {{/each}}
    </ul>
</div>

<div id="inputArea">
    <hr/>
    <h2>Add a DO to be Done</h2>
    <!-- bmc: this form captures a burger name and inserts it into the database as an uneaten burger -->
    <form class="create-update-form"
          action="/add"
          method="POST">

        <div class="form-group">
            <label for="burgerName">What DO do you need done?</label>
            <br/>
            <input type="text"
                   id="burgerName"
                   name="name">
        </div>

        <button type="submit">Add this DO to the List</button>
    </form>
</div>
<hr/>

