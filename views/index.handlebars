<h1>Honey Do List</h1>

<div class="image">
    <img src="assets/images/HoneyDoListImage.png">
</div>

<p>Enter a task that you want your honey to do and it will show up in the honey do list. Then click on "done" to mark it done and have it move to the done list.</p>
<hr/>
<div id="uneatenList"
     class="leftSide">
    <h2>Honey, do this...</h2>
    <table>
        <!-- bmc: handlebars #each will loop through all the uneatenBurgers in the database -->
        {{#each uneatenBurgers}}
            <tr>
                <td>
                    do#{{id}}: {{food}}
                </td>
                <td>
                    <form action="/{{id}}?_method=PUT"
                          method="POST">
                        <!-- bmc: button will launch the put method to change the eaten column in the database to true -->
                        <button class='eat-in-database'
                                id={{id}}>done
                        </button>
                    </form>
                </td>
            </tr>
        {{/each}}
    </table>
    <ul>

    </ul>
</div>
<div id="eatenList"
     class="rightSide">
    <h2>Done!</h2>
    <ul>
        <!-- bmc: list of eaten burgers like the list of uneaten ones above -->
        {{#each eatenBurgers}}
            <li>
                do#{{id}}: {{food}}
            </li>
        {{/each}}
    </ul>
</div>

<div id="inputArea">
    <hr/>
    <h2>Add a DO</h2>
    <!-- bmc: this form captures a burger name and inserts it into the database as an uneaten burger -->
    <form class="create-update-form"
          action="/"
          method="POST">

        <div class="form-group">
            <label for="burgerName">Enter your next DO for your honey:</label>
            <br/>
            <input type="text"
                   id="burgerName"
                   name="name">
        </div>

        <button type="submit">add DO</button>
    </form>
</div>
<hr/>

